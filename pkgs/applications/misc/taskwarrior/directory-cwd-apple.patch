--- task-2.3.0/src/Directory.cpp
+++ task-2.3.0/src/Directory.cpp
@@ -163,6 +163,13 @@
   std::string result (buf);
   free (buf);
   return result;
+#elif __APPLE__
+  char *buf = getcwd (NULL, 0);
+  if (buf == NULL)
+    throw std::bad_alloc ();
+  std::string result (buf);
+  free (buf);
+  return result;
 #else
   char buf[PATH_MAX];
   getcwd (buf, PATH_MAX - 1);
